# ЧЕК-ЛИСТ БЕЗОПАСНОСТИ (заполненный)

## 1. Хранение данных

| № | Проверка | Статус | Примечание |
|---|----------|--------|------------|
| 1.1 | Пароль не сохраняется в базе данных | ✅ | Пароль `1234567` не сохранился в БД |
| 1.2 | В базе утечек хранятся только хэши паролей, а не сами пароли | ✅ | В логах сервера виден только хэш, не пароль |
| 1.3 | Нет файлов с логами, содержащими пароли | ✅ | Поиск `*.log` ничего не дал |

---

## 2. Логирование

| № | Проверка | Статус | Примечание |
|---|----------|--------|------------|
| 2.1 | Пароль не выводится в консоль (print, console.log) | ✅ | В логах терминала нет `1234567`, только хэш |
| 2.2 | Пароль не записывается в файлы логов | ✅ | Файлов `.log` в проекте нет |
| 2.3 | В логах сервера нет текста введённого пароля | ✅ | Проверено по логам |
| 2.4 | В логах браузера (F12) нет пароля | ✅ | Консоль браузера не содержит пароль |

---

## 3. API безопасность

| № | Проверка | Статус | Примечание |
|---|----------|--------|------------|
| 3.1 | API не возвращает пароль в ответе | ✅ | В ответе Postman нет поля `password` |
| 3.2 | Передача пароля только методом POST (не GET) | ✅ | GET-запрос вернул `405 Method Not Allowed` |
| 3.3 | Пароль не передаётся в URL | ✅ | GET запрещён → в URL не передать |
| 3.4 | При ошибке валидации не возвращается пароль | ❌ | БАГ: пустой пароль не вызвал ошибку, вернул `weak` |
| 3.5 | API использует HTTPS (при деплое) | ⬜ | Будет проверено после деплоя |

---

## 4. Фронтенд (UI)

| № | Проверка | Статус | Примечание |
|---|----------|--------|------------|
| 4.1 | Поле ввода пароля имеет тип `password` (символы скрыты) | ✅ | На скриншоте видны звёздочки |
| 4.2 | Пароль не отображается на экране после проверки | ✅ | Результат показывает только оценку, пароль скрыт |
| 4.3 | Пароль не сохраняется в localStorage / sessionStorage | ✅ | `localStorage` пуст или содержит только тему (`light`) |
| 4.4 | Пароль не виден в исходном коде страницы | ✅ | Поиск `1234567` в Elements ничего не дал |

---

## 5. Общие проверки

| № | Проверка | Статус | Примечание |
|---|----------|--------|------------|
| 5.1 | Нет возможности отправить пароль GET-запросом | ✅ | GET вернул `405 Method Not Allowed` |
| 5.2 | Нет хардкода паролей в коде | ⬜ | Требуется код-ревью  |
| 5.3 | База утечек не содержит исходных паролей | ✅ | В логах только хэши, исходных паролей нет |

---

## Результаты проверки

| Показатель | Количество |
|------------|------------|
| Всего проверок | 16 |
| Выполнено (✅) | 13 |
| Баг (❌) | 1 |
| Не проверено / ждёт (⬜) | 2 |

---

## Список замечаний (баг)

| № | Проверка | Замечание |
|---|----------|-----------|
| 3.4 | Ошибка валидации | Пустой пароль не вызывает ошибку (должен возвращать `400 Bad Request`) |

---

## Примечания

- База данных `test.db` пуста. Проверка утечек будет протестирована в спринте 3 после наполнения базы синтетическими данными.
- Пункты 3.5 (HTTPS) и 5.2 (хардкод) будут проверены позже.